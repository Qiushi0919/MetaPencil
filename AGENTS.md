# MetaPencil agent instructions

这些规则适用于人在回路的 Codex、ChatGPT 和其他代码代理。

## 每次任务开始前

1. `git pull --ff-only`，确认仓库和当前分支。
2. 完整阅读本文件、`AI_CONTEXT.md`、`PROJECT_STATE.json` 和 `AI_SYNC.md` 最新记录。
3. 查看相关 Issue；除只读审阅外，在 `feature/*`、`fix/*` 或 `analysis/*` 分支工作。
4. 检查工作区现有改动并保护用户未提交的内容。

## 不得静默改变的约定

- 距离/方位轴及其正负号、斜距/地距换算；
- `exp(+j\phi)` 与传播双程相位约定；
- 米、秒、Hz、弧度/角度单位；
- 振幅、功率、dB 和归一化参考；
- 理论系数、图像模板、复原始回波、全波和实测的证据等级；
- 1-bit/2-bit 状态含义、驻留比例和循环时延定义。

若确需改变，先写清变更前后公式、原因、受影响基线和迁移方式。

## 修改范围

- 不做无关格式化或大规模重构；不删除历史结果。
- 不在未验证时“顺手修正”公式或坐标；诊断和修复分开提交。
- 不把探索性输出直接覆盖到 `outputs/` 的已有基线；使用新目录并附运行清单。
- 不把图像域幅度叠加描述成严格复电场相干叠加。
- 不把理想仿真描述为真实超表面或雷达实验。

## 验证和交付

1. 至少运行与风险相称的最小测试；保留真实日志，不声称未运行的测试通过。
2. 结果需记录 commit、参数、归一化、输出路径和模型层级。
3. 更新 `PROJECT_STATE.json`、在 `AI_SYNC.md` 顶部追加记录、更新 `CHANGELOG.md`。
4. 为可审阅工作生成 `review_packets/YYYYMMDD_<shortsha>_<topic>/`。
5. 提交信息使用 `type(scope): summary`；推送并通过 PR 合并，禁止 force push。

## 当前 P0 模型边界

- `tile_map` 的空间位置目前没有进入有限口径散射，只通过每个通道的计数形成权重。
- 每个通道目前乘整架飞机的复回波，而非物理分区对应的散射点子集。

在声称“宏格空间排布优化”“口径栅瓣抑制”或“真实分区共形响应”前，必须先实现并验证位置感知模型。

