# MetaPencil AI context

## 研究问题

MetaPencil 研究时变数字编码超表面如何把一个或多个飞机的 SAR 复回波重构为可编程假目标群。当前论文切口是：在理想单极化 2-bit 四相位约束下保留指定 `(+1,+1)` 通道，同时用四组循环时延的相量叠加抑制 `(-3,+1)`、`(+5,+1)`、`(+1,-3)`、`(+1,+5)` 外侧十字副像，再把外层面积自由度分配给多个目标通道。

长期目标是约 2 m × 2 m 的无人机模型共形可编程超表面；全波共形模型、FPGA 控制、宽带斜入射单元和实物实验尚未完成。

## 统一入口与链路

主要入口：

`source/2 暑假_两个月仿真任务/Week 5 0815之前/SAR假目标自由度与物理阵列/run_full_array_freedom_study.m`

```text
歼-36 二维散射点
  -> 中心飞机 LFM 复原始回波 sr_center
  -> 目标通道 offsets 与外层 tile_map
  -> 四态时序 + 距离/方位循环时延平均
  -> 面积加权二维响应 response
  -> sr_center .* response（多机时先平移各自复回波再求和）
  -> 自定义 RD 聚焦
  -> SAR 图、控制表和基线结果
```

关键参数：10 GHz、60°、500 m、80 m/s、0.25 m 地距/方位分辨率、8 m 目标网格、四态 `[0,90,180,270]°`、时延 `[0,1/10,5/6,14/15]T`。

## 已确认的两个关键模型边界

### 1. `tile_map` 当前只贡献计数，不贡献空间位置

代码证据：

- `run_full_array_freedom_study.m:409-420` 生成蛇形 `tile_map`。
- `run_full_array_freedom_study.m:422-426` 在 `compilePhysicalResponse` 中只执行 `histcounts(tile_map(:), ...)`，再计算 `weights = counts/numel(tile_map)`。
- `run_full_array_freedom_study.m:430-449` 对每个通道生成一个快/慢时间响应，并只以 `weights(channel)` 加权求和。
- `tile_map` 的行列坐标仅在 `455-463` 行输出为控制表，没有进入传播距离、口径相位或散射点选择。

结论：在当前实现中，只要各通道计数相同，不同的宏格空间排列会产生相同 `response`。图中“均匀交织/蛇形排布”是控制方案示意，不是已经仿真的有限口径空间散射。任何关于排布抑制栅瓣或空间旁瓣的结论都需要新的位置感知模型。

### 2. 每个 H_k 当前作用于整架飞机响应

代码证据：

- `run_full_array_freedom_study.m:92-109` 先把全部选中散射点累加为同一个完整中心飞机回波 `sr_center`。
- 单机链路在 `141-152` 行编译所有通道的加权总响应，然后执行 `sr_meta = sr_center .* response`。
- 双机链路在 `218-228` 行对完整 `sr_center` 做位置平移，再让完整源回波乘对应码本响应。
- 四机链路在 `248-263` 行重复同样操作。

结论：当前每个 H_k 获得的是面积权重乘整架飞机散射函数，等效假设通道子单元在整个有效口径上足够细且均匀交织。代码没有把飞机表面散射点分配给具体宏格，也没有计算每个宏格的局部照射、局部相位中心或遮挡。下一版物理模型应建立“宏格几何—散射点/三角面—局部状态时序”的显式映射。

## 模型证据层级

| 层级 | 状态 | 可以支持的结论 |
|---|---|---|
| L0 理论傅里叶系数 | 已实现 | 量化谐波位置、理想相量零陷 |
| L1 可分离时序响应 | 已实现 | 理想距离/方位二维通道组合 |
| L2 点散射体复回波 + RD | 已实现 | 当前理想系统级 SAR 基线 |
| L3 位置感知有限口径分区 | 未实现 | 真实宏格排布、局部散射和空间栅瓣 |
| L4 全波/共形/硬件 | 未实现 | 单元互耦、斜入射宽带、控制误差和实测性能 |

## 已验证与未解决

已验证：1-bit/2-bit 高阶谱、四时延 `-3/+5` 理想零陷、二维可分离系数、点散射体复回波/RD、多目标通道编译、控制表导出。

未解决：位置感知空间分区、统一能量守恒和 PSLR/ISLR、非理想状态误差、快慢时间硬件调度、宽带斜入射四态单元、互耦/极化/噪声/杂波、共形全波和实测。

## 结果解释红线

- PNG 上低于显示门限不等于能量为零。
- 逐图归一化结果不能用于绝对幅度比较。
- 历史 `run_target_freedom_study.m` 的图像幅度模板平移仅用于构思，不能证明严格相干电磁叠加。
- H_k 是目标通道，不是新的 PIN 状态；内层 4×4 延迟基元与外层通道阵列不能混淆。

## 阅读顺序

1. `PROJECT_STATE.json`
2. `AI_SYNC.md` 最新记录
3. `docs/handoff/06_FORMULA_CODE_MAPPING.md`
4. `docs/handoff/07_RESULTS_TRACEABILITY.md`
5. `docs/theory/MODEL_LEVELS.md`
6. 当前统一入口和相关 Issue

