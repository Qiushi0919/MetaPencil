# No third-party Python packages are required by handoff_tools/*.py.
# Python >= 3.9 is sufficient for run_smoke_test.py.
# build_snapshot.py uses pathlib.Path.is_relative_to and therefore needs Python >= 3.9.
