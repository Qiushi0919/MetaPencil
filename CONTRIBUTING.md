# Contributing to MetaPencil

1. 阅读 `AGENTS.md`、`AI_CONTEXT.md`、`PROJECT_STATE.json` 和最新 `AI_SYNC.md`。
2. 从最新 `main` 创建 `feature/*`、`fix/*` 或 `analysis/*` 分支。
3. 用 Issue 写明模型层级、坐标/相位/归一化约定和验收标准。
4. 只修改任务相关文件；不要覆盖基线结果或进行无关格式化。
5. 运行最小测试并记录真实命令、参数、commit hash 和输出。
6. 更新项目状态、同步日志、变更记录和 review packet。
7. 通过 Pull Request 合并；禁止 force push。

提交信息使用 `type(scope): summary`，推荐类型：`feat`、`fix`、`test`、`docs`、`analysis`、`data`、`chore`。

