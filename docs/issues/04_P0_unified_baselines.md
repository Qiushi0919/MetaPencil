# P0 — Unify 1-bit, ordinary 2-bit and cancelled 2-bit baselines

Labels: `P0`, `reproducibility`, `paper-1`

## Goal

Run the three principal schemes from one immutable parameter snapshot and one common RD/normalization path.

## Acceptance

- Same radar, scatterers, axes, noise setting and unmodulated reference.
- Explicit amplitude versus power dB convention.
- One manifest links inputs, source commit, commands and outputs.
- Historical presentation-only per-figure normalization remains separate and labeled.

