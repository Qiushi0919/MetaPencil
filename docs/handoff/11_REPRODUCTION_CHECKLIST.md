# 复现检查清单

## 已完成

- [x] MATLAB/Python/系统环境已识别并记录。
- [x] 当前主入口所需的 Week4 `airplane_scatter_points.mat` 已按原相对结构收录。
- [x] 所有 MATLAB/Python/MJS 源码已收录；大文件规则未排除源代码。
- [x] `handoff_tools/run_smoke_test.m` 与 `.py` 可运行。
- [x] 烟雾测试实际返回 0，日志中为 `SMOKE_TEST_STATUS=PASS`。
- [x] 2-bit `C+1`、4时延 `S+1/S-3/S+5`、二维系数与1→2面积分配已断言。
- [x] 当前模型生成脚本 `ship_trans.m` 固定 `rng(2026)`；当前主入口本身无随机数。
- [x] 主入口自动创建输出和控制表目录。
- [x] 主入口使用未调制单机统一参考，不是逐图0dB。
- [x] 0818 PPT 26页和全部备注已提取。
- [x] 敏感信息已扫描；ZIP副本的绝对个人路径已脱敏。
- [x] 原始1056文件已记录路径、大小、SHA256、收录状态和原因。
- [x] 被排除的大MAT/FIG仍有原路径、SHA256和PNG/CSV替代物记录。
- [x] 原项目不是Git仓库的事实已记录。

## 部分完成

- [~] 完整主入口具有相对路径，但历史PPT MJS构建器仍需在新机器配置artifact-tool路径。
- [~] 关键历史结果具有代码和PNG/CSV；本次没有重新运行全部700/3000点高精度SAR。
- [~] 参数在代码和本文档中可追溯，但历史时间戳目录没有统一的机器可读参数JSON。
- [~] 当前主结果统一参考；部分历史/展示脚本仍逐图归一化。
- [~] 多机在复原始数据层叠加，但真实同步和相对相位不确定性未建模。
- [~] 手机草图入口可运行；若输入图缺失会回退到Z形，需人工确认输入文件。

## 尚未完成

- [ ] 用交接副本完整重跑 1-bit、普通2-bit、相消2-bit三条同参数论文基线。
- [ ] 自动输出目标区积分功率、全图功率、PSLR、ISLR和分辨率。
- [ ] 用实测四态复反射替代理想等幅90°状态。
- [ ] 验证二维非可分离和互耦条件。
- [ ] 运行全波共形无人机、FPGA控制或真实实验。

## ZIP 验收

- [x] 根目录包含 `00_README_FIRST.md` 和所有13份交接文档。
- [x] 包含 `project_state.json`、`file_manifest.csv`、`original_tree.txt`。
- [x] 包含源码、配置、小数据、模型、关键结果、PPT/PDF和参考文献。
- [x] 包含烟雾测试日志与错误日志。
- [x] 已生成 ZIP 内部文件的 `CHECKSUMS_SHA256.txt`。
- [x] ZIP 已通过 `unzip -t` 完整性测试。

最后两项由打包步骤实际验证；如从本包二次修改文件，应重新生成校验和与ZIP。
